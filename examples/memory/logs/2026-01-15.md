# Log 2026-01-15

## 🛠️ T1 Receipt Scanner

### Completed

- **Step 0 flagged the main file at 18 KB** with 7 dated "completed" sections →
  split into `core.md` + `state.md`.
  - `core.md` kept the purpose, the definition of done, the 4 inviolable rules
    and the package seams. Every dated line was stripped out of it.
  - `state.md` written fresh (not migrated): current phase, resume point, 3
    TODOs, last-30-days milestones.
  - The November OCR engine comparison moved to
    `archived/2025-11_ocr_engine_selection.md` — it is a *decision*, still worth
    keeping, but it is not current state.
- Traced the 17 Q4 failures to a single cause: the Costa parse rule anchors on
  the first `TOTAL`-prefixed line, which is the subtotal. Wrote a failing test at
  `parse/rules/costa_test.go:41` before stopping.

### Stopped at

- Failing test committed on branch `fix/costa-total-anchor`. The fix itself is
  not written.

### Next time

- Change the anchor to the **last** match in the block, make
  `costa_test.go:41` pass, then re-run Q4 and diff against the hand-corrected
  CSV. Byte-identical on merchant/date/total/VAT or it is not fixed.

### Notes

- ⚠️ `MEMORY.md` is at 2.4 KB — fine, but December's log lines are still in
  "Recent Log". Next session should roll them into `logs/archive.md`.
- Still waiting on the accountant re: zero-rated VAT column (6 days). Not
  blocking the Costa fix.
