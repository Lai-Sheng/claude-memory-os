---
name: user_profile
description: Who the user is — role, stack, working context. Always loaded.
metadata:
  type: user
---

# User Profile

> Keep this short and **stable**. Anything that changes month to month belongs in
> a project `state.md`, not here.

- **Role**: {{e.g. embedded software engineer at a hardware company}}
- **Primary stack**: {{e.g. C / C++ / Unity / C#, VS Code, Windows}}
- **Working context**: {{e.g. works on personal projects on weeknights; long
  sessions that often cross midnight — "today" may mean "this session"}}
- **Domain background**: {{what they already know, so explanations can skip it}}
- **Known constraints**: {{e.g. no admin rights on the work machine}}

## Working preferences

- {{e.g. wants a recommendation, not a survey of options}}
- {{e.g. quotes and prices always in local currency}}
